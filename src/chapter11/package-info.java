
/**
 * @author jverson
 * 持有对象
 * 所谓持有对象就是保存和持有对象的引用，实现的方式包括List、Queue、Map、Set，这些类库也被称为“集合类”。
 * java容器类库的用途就是“保存对象”，可以划分成两个不同的概念：
 * 1. Collection。一个独立元素的序列，包括List、Set及Queue
 * 2. Map。一组成对的“键值对”对象
 * 
 * 注意尽管数组也可以保存一组对象（也可以是基本数据类型），但由于其尺寸固定使用不够灵活并不属于java的集合类库。
 */
package chapter11;